package org.windowsprint.component;

import java.util.List;

public class Windows extends Container {
    public Windows(String name, List<IComponent> children) {
        super(name, children);
    }
}
